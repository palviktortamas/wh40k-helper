# dependencies
node_modules/
.pnp/
.pnp.js

# build output
dist/
build/
.vite/
*.tsbuildinfo

# env / local config
.env
.env.*
!.env.example
.claude/settings.local.json

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
desktop.ini

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# test / coverage
coverage/

# GAME DATA — never commit GW/community datafiles (see spec 4.2).
# Anchored to the repo root so it never swallows source dirs like src/data/.
/data/
*.cat
*.catz
*.gst
Orks.json
"Warhammer 40,000.json"
*-mfm.yaml
missions-ca-*.json

# The mission deck fetched from Wahapedia at build time (scripts/fetch-mission-deck.mjs); never committed.
/public/missions-ca-*.html
